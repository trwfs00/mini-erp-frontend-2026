import {
  AlertTriangle,
  CheckCircle2,
  Info,
  XCircle,
} from "lucide-react";

export const IconCheck = <CheckCircle2 size={18} strokeWidth={2.25} />;
export const IconError = <XCircle size={18} strokeWidth={2.25} />;
export const IconInfo = <Info size={18} strokeWidth={2.25} />;
export const IconWarning = <AlertTriangle size={18} strokeWidth={2.25} />;
