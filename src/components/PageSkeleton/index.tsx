// File kept intentionally empty — skeletons now live inline in each page
// to mirror that page's actual layout, and the InstantTable component
// renders skeleton rows when `isLoadingInitial` is true.
export {};
